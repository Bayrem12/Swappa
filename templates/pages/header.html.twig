<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/v/bs5/dt-2.0.3/datatables.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_pages') }}">Swappa</a>

        <button class="navbar-toggler" type="button" 
                data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-label="Toggle navigation" aria-expanded="false" aria-controls="navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_pages') }}">Iphones</a>
                </li>
                
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_about') }}">About</a>
                </li>
                <li>
                    <!-- Cart Icon in Navbar -->
                    <div class="cart-icon ms">
                        <a href="{{ path('app_cart') }}" class="btn btn-primary position-relative">
                            <i class="bi bi-cart"></i>
                            <span class="cart-badge badge bg-danger rounded-pill">{{ app.session.get('cart')|length }}</span>
                        </a>
                    </div>
                </li>
                <li>
                    {% if app.user %}
                <div class="d-flex">
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-light">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </div>
                {% endif %}
                </li>
                
            </ul>
        </div>
    </div>












    
</nav>

<style>
    /* Add to your CSS file */
    .btn-logout {
        background: transparent;
        border: none;
        padding: 8px 12px;
        display: flex;
        align-items: center;
        gap: 8px;
        color: rgba(255, 255, 255, 0.75);
    }

    .btn-logout:hover {
        color: white;
        background: rgba(255, 255, 255, 0.1);
    }

    .logout-form {
        display: flex;
        height: 100%;
    }

    .logout-item {
        display: flex;
        align-items: center;
    }

    .logout-text {
        font-size: 1rem;
    }

    /* Match nav-link hover transition */
    .btn-logout {
        transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out;
    }
</style>